using System;
using System.Linq;

namespace PigLatin
{
    public class Program
    {


        //Not sure if I was supposed to put something in here or just test the methods with the actaul/expected list

        //    string input = GetInput("Enter a word or phrase to be translated to pig latin");
        //}

        //public string GetInput(string prompt)
        //{
        //    Console.WriteLine(prompt);
        //    string input = Console.ReadLine().ToLower().Trim();
        //    return input;


    }
}
